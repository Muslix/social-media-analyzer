[pytest]
markers =
    network: tests that require outbound network access to external services
    llm: tests that invoke the Ollama language model
    slow: tests that may take longer to run (LLM calls, end-to-end checks)
