# ════════════════════════════════════════════════════════════════════
# Social Media Market Impact Analyzer
# ════════════════════════════════════════════════════════════════════
# Python dependencies for Social Media Market Impact Analyzer

# Core
requests>=2.31.0
beautifulsoup4>=4.12.2
feedparser>=6.0.10
pymongo>=4.5.0
colorama>=0.4.6
python-dotenv>=1.0.0
pycoingecko>=3.1.0

# Rate limiting
ratelimit>=2.2.1
backoff>=2.2.1

# LLM Integration (Ollama API)
# Ollama itself runs as separate service (see docker-compose.yaml)
# We only need requests to communicate with Ollama API

# Optional: For future spaCy NER training
# spacy>=3.7.0
# spacy-transformers>=1.3.0

# Development Dependencies (optional)
# pytest>=7.4.0
# black>=23.0.0
# flake8>=6.0.0
